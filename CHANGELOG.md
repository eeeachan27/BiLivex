# 更新日志

## v2.1.0（2026-09-06）

新功能与改进：
1. 参考 [DouyuEx](https://greasyfork.org/zh-CN/scripts/394497-douyuex-%E6%96%97%E9%B1%BC%E7%9B%B4%E6%92%AD%E9%97%B4%E5%A2%9E%E5%BC%BA%E6%8F%92%E4%BB%B6) 的「同步时间」思路，在 B 站播放器底部原生操作栏的直播持续时间右侧新增同步按钮，并按 B 站播放器结构独立适配。
2. 点击后优先跳到最后一段本地缓冲区末端前 0.05 秒；缓冲区不可用时回退到播放器可拖动区间末端，避免卡在 MSE 分段边界。
3. 同步操作只调整 `video.currentTime`，不会主动调用播放或暂停，也不会点击、替换或拦截 B 站任何原生控件。
4. 按钮跟随相邻原生图标样式，并支持控制栏延迟挂载、卸载重建、切换直播间和特殊直播间；重复初始化只保留一个按钮。
5. 根据实机反馈重绘同步图标：外圈以 24 × 24 视区中心对称，箭头移至左上角，缩短中心时针并减细描边，避免箭头与时针挤在一起。
6. 新增目标区间单元测试，以及按钮位置、图标几何、原生视觉、同步行为、原生控件隔离和控制栏重建等浏览器测试。

## v2.0.3（2026-09-05）

修复与改进：
1. 将面板和油猴元数据使用的 logo 压缩为内置资源，脚本体积减少约 110 KB。
2. 移除 logo 的外部网络依赖，断网或大陆网络环境下仍可正常显示。
3. 油猴页图标与面板统一使用同一份压缩资源，避免图标来源不一致。

## v2.0.2（2026-09-05）

修复与改进：
1. 恢复房间号识别，自动更新检查可从普通链接、`blanc` 链接、查询参数和页面属性取得房间号。
2. 删除引用未定义常量的面板死代码，并加入 ESLint 静态检查。
3. 配置改为增量保存，使用 Tampermonkey 跨实例监听同步主题和开关；收藏操作只写独立收藏键，不再覆盖主配置。
4. `+1` 发送结果改为“已触发 / 已确认 / 未确认超时”等真实语义，不会因确认超时自动重发。
5. 聊天区与漂浮弹幕观察器仅处理新增节点，同帧合并；页面卸载时清理观察器和定时器。
6. 一键点赞分别显示原生点击触发次数和可观察反馈次数，不再将无反馈点击表述为成功。
7. 新增 Node 单元测试、Playwright 本地浏览器夹具和 ESLint；真实发送、真实点赞仍需单独授权。

本版本建议旧版本用户更新，以获得新的原生 `+1` 发送逻辑和跨 iframe 稳定性。

## v2.0.1（2026-09-05）

修复与改进：
1. 修复鼠标移出漂浮弹幕后蓝色悬停框未清理的问题：悬停高亮只绘制在 resident 副本上，不再污染 B 站复用的原弹幕节点
2. 复刻参考项目的原生 `+1` 发送路径，补齐全屏输入框 `#fullscreen-danmaku-vm input.chat-input` 和发送按钮 `.send-danmaku`，移除接口发送兜底，确保 B 站播放器保留本人弹幕原生蓝色标识
3. 支持普通图片表情的 `alt`、`data-resource-id` 与资源哈希解析，支持文字和普通表情混合内容
4. 支持特殊表情资源哈希识别，并通过 B 站原生表情面板定位、切换标签和点击发送
5. 进一步迁移参考项目的原生事件链：使用输入框所属窗口构造 `InputEvent`，Enter 兜底同时派发 `keydown` 与 `keyup`，并增加全屏容器内发送按钮兼容选择器
6. 悬停时缓存结构化弹幕载荷，避免 B 站回收或复用源节点后 `+1` 错读成下一条内容；即使源节点已移除或禁用，离开时仍清理暂停状态和内联可见性
7. 增加悬停初始化失败回滚并删除未挂载的旧按钮实现，避免异常路径遗留冻结节点
8. 新增零依赖 2.0.1 回归检查，覆盖版本号、全屏原生发送、完整键盘事件、接口路径移除、表情链路、载荷快照和悬停样式隔离

## v2.0.0（2026-09-05）

重构与优化：
1. 重构 `+1` 发送链路，优先使用 B 站原生聊天输入框、事件和发送按钮，接口仅作为原生输入框不可用时的兜底
2. 重构漂浮弹幕悬停底层逻辑，使用独立 resident 副本支持长时间悬停保活，兼容普通直播间、iframe、网页模式和全屏模式
3. 删除脚本伪造的本人弹幕蓝框，保留 B 站原生本人弹幕标识，避免相同文案误判为本人发送
4. 自动更新检测除每小时检测外，首次打开直播间和 SPA 切换新房间时立即检测
5. 保持收藏、主题色、小尾巴、点赞、界面和按钮等既有功能

## v1.1.9（2026-09-04）

修复已知 BUG：
1. 修复：iframe 直播间（活动 / 特殊直播间）网页大窗口 / 网页全屏模式下，悬停弹幕后弹幕本体消失、只剩操作栏的问题——悬停期间弹幕与操作栏同时完整可见，移开鼠标后原弹幕恢复滚动
2. 修复：自己发送或 +1 过的弹幕文案，被其他人再次发送时会被误标蓝色边框的问题——现只有自己真正发送的弹幕才带蓝色边框，他人发送相同内容不再误标

## v1.1.8（2026-09-04）

修复已知 BUG：
1. 修复：iframe 直播间网页大窗口 / 网页全屏模式下，悬停弹幕后「+1 / 收藏」操作栏不显示或被放大播放器层遮挡的问题——操作栏与悬停弹幕统一挂载，任何窗口模式下都完整可见
2. 修复：网页全屏状态下悬停弹幕后弹幕本体消失、只剩操作栏的问题——悬停期间弹幕与操作栏同时完整可见，移开鼠标后原弹幕恢复滚动
3. 修复：全屏 / 网页全屏模式下自己发送的漂浮弹幕没有蓝色边框的问题——本人弹幕在悬停与取消悬停后均保持蓝色边框
4. 修复：iframe 直播间贴近画面边缘的弹幕（文字大部分已滚出直播画面区域）悬停后直接消失的问题——边缘弹幕悬停后按当前位置完整显示
5. 修复：悬停带表情图 / 舰长徽章等富文本内容的弹幕时表情被放大、内容显示异常的问题——悬停弹幕与原弹幕外观保持一致

## v1.1.3（2026-09-03）

修复已知 BUG：
1. 修复：进入直播间时偶现的「操作栏被裁剪或遮挡」问题——刷新页才能恢复的现象消失，现在进入一次即可生效
2. 修复：弹幕前面带舰长徽章 / 表情图等富文本前缀时悬停会看到两条重复且文本错位的问题——副本完整保留徽章等子节点结构，与源弹幕内容对齐
3. 修复：自动更新检测过严——同一天发布的新版本容易因「6 小时内查过」而看不到提示；同时网络瞬时失败会被锁 6 小时不再重试——检测窗口缩短为 1 小时，且仅请求成功才记录检查时间，失败可立即重试

## v1.1.2（2026-09-03）

修复已知 BUG：
1. 修复：网页全屏下屏幕上方、左侧、右侧约 1/5 区域内的漂浮弹幕，悬停后的操作栏无法正常显示——现边缘区域的弹幕悬停后，操作栏也会在屏幕内完整露出，不再被画面边界裁切或遮挡
2. 修复：左右两侧弹幕悬停固定后，部分操作栏出现被裁剪或遮挡的痕迹——现操作栏与弹幕分层展示，弹幕本身仍不超出直播画面，操作栏始终完整可见
3. 修复：悬停后的弹幕在画面上/下区域的透明度与原弹幕不一致——现悬停弹幕保持与原弹幕一致的透明度，观感自然统一

## v1.1.1（2026-09-03）

新功能：
1. 新增自动检查更新：发现新版本时在悬浮球旁弹出小窗提醒，提供「忽略」与「更新」两个选项；点击「更新」在新标签页打开油猴更新页完成升级，点击「忽略」后该版本不再提示

修复已知 BUG：
1. 修复：弹幕悬停后保持指针不动滚动页面，悬停弹幕会吸附跟随指针、原悬停位置残留一个透明度更低的弹幕副本——现悬停状态下滚动页面直接判定为取消悬停，恢复弹幕正常滚动，且任何时刻只保留一个悬停弹幕，不再出现两个弹幕共存

## v1.1.0（2026-09-01）

新功能：
1. 新增弹幕收藏夹：支持从聊天区和漂浮弹幕快捷收藏常用弹幕
2. 支持在收藏夹中搜索、编辑、删除、导入和导出弹幕内容
3. 点击收藏内容可填入聊天框，点击「+1」可发送同内容弹幕

修复已知 BUG：
1. 修复：收藏弹幕后收起主菜单，收藏内容会消失——收藏数据改为独立保存，收起菜单、拖动悬浮球或切换设置都不会再覆盖已收藏的内容
2. 修复：悬浮窗拖动手感松软、不跟手——拖动过程中实时跟随鼠标位置，松手后再执行边缘吸附

## v1.0.11（2026-08-29）

新功能：
1. 新增：自己发送的弹幕会在聊天区用蓝色边框标出，一眼认出是自己发的

修复已知 BUG：
1. 修复：直播间聊天区在分栏画面中时，小尾巴设置不生效（自己发送和 +1 都不会带上尾巴内容），现始终使用最新的小尾巴设置

## v1.0.10（2026-08-15）

修复已知 BUG：
1. 修复：iframe 直播间弹幕悬停判定区域与实际弹幕位置不符，现判定区域与弹幕位置对齐
2. 修复：iframe 直播间选中弹幕与 +1 按钮之间存在空隙导致无法点击，现悬停可顺畅移动到 +1 按钮
3. 修复：iframe 直播间从弹幕移动到 +1 按钮时悬停被取消，现跨页面移动不中断悬停
4. 修复：iframe 直播间全屏状态下点击 +1 无法发出弹幕，现改为接口直发，发送更稳定
5. 修复：iframe 直播间边缘弹幕悬停后文字会超出直播画面，现限制在画面范围内
6. 修复：聊天区弹幕悬停不再改写 B 站原生样式，避免覆盖直播间的原生显示效果

## v1.0.9（2026-08-14）

修复已知 BUG：
1. 修复：长时间悬停弹幕时弹幕文字会消失（判定框和 +1 按钮仍在），现悬停期间弹幕文字持续显示
2. 修复：悬停漂浮弹幕时暂停原始动画，离开后按原始进度恢复，弹幕继续滚出屏幕并自然结束
3. 修复：评论区手动点击发送或按 Enter 时，在发送边界追加一次小尾巴；用户输入过程不改写，脚本 +1 与自动发送行为保持正确
4. 修复：底部/顶部特殊定位弹幕纳入候选扫描，恢复正常悬停、选中和 +1
5. 修复：关闭功能时残留弹幕节点不回插 B 站原始弹幕轨道
6. 修复：悬停期间弹幕动画结束后仍保持冻结状态，避免悬停时弹幕提前消失

## v1.0.8（2026-08-13）

修复已知 BUG：
1. 修复：活动直播间悬浮球拖动时会弹来弹去、难以自由定位——同一页面多个悬浮球控制相互干扰，现只保留一个控制器，拖动位置可自由保存
2. 修复：活动直播间悬浮球展开菜单后会被强制吸附到侧边，拖动到的位置被改动，现展开菜单仅保持在当前可视位置

## v1.0.7（2026-08-12）

修复已知 BUG：
1. 修复：悬浮球吸附右侧时，展开菜单后收起会向左偏移（菜单展开前的锚点位置计算错误），现收起后悬浮球保持在吸附侧原位
2. 修复：直播间漂浮弹幕会轻微抖动，悬停后的弹幕正常无抖动
3. 修复：对未完全显示在直播画面内的弹幕悬停时，弹幕显示会超出直播画面，现已限制在画面范围内
4. 修复：鼠标划过画面左上角仍会闪现已结束的弹幕，已结束/不再播放的弹幕不再触发悬停效果
5. 修复：悬停的弹幕与 +1 按钮之间存在空隙，鼠标下移去点 +1 时会被判定为取消悬停，现悬停区域内可顺畅移动到 +1 按钮
6. 修复：部分场景下 +1 按钮不显示（弹幕文字被画面边界裁剪时按钮一并被隐藏），现按钮始终跟随弹幕显示

## v1.0.6（2026-08-11）

修复已知 BUG：
1. 修复：开启插件后直播间漂浮弹幕数量明显变少（评论区能看到消息但画面中弹幕大量缺失）——插件不再干扰直播间弹幕的正常滚动与显示，弹幕数量恢复正常
2. 保留：自动清理残留的悬停冻结弹幕，长期使用不卡顿

## v1.0.5（2026-08-11）

修复已知 BUG：
1. 修复：鼠标在直播画面左上角滑移导致弹幕卡死无法消除——已滚出画面左侧/即将结束的弹幕不再被悬停冻结（让其自然消失）；快速滑移时自动释放已冻结弹幕，不再被悬停热区困住；兜底清理钳制在画面左边缘的残留弹幕
2. 保留：自动清理生命周期已结束的历史弹幕（动画结束或已滚出画面左侧的残留节点）

## v1.0.4（2026-08-11）

修复已知 BUG：
1. 修复：鼠标在直播画面左上角滑移导致历史弹幕卡死无法消除——弹幕生命周期结束后未清理会残留在画面左侧，反复被悬停命中并冻结；现在自动清理生命周期已结束的弹幕，且不再对已消失的弹幕执行保活
2. 修复：悬浮球吸附右侧时展开菜单出现在左侧（用户已拖拽定位后不再强制避让聊天区，尊重选边意图）

## v1.0.3（2026-08-11）

UI 与交互优化：

1. 折叠态悬浮球替换为圆形 logo 图（之前为粉色+文字），视觉更清爽，操作逻辑不变
2. 修复：特殊直播间面板被直播间边框遮蔽问题，提升面板层级
3. 修复：特殊直播间悬浮球可拖动至视口任意位置，不再受直播画面范围限制
4. 修复：悬浮球在右侧时展开菜单方向错乱，根据小球位置智能选边（始终靠小球一侧贴边）
5. 修复：菜单展开超出网页边界问题，自动吸附到视口边缘不溢出
6. 展开后的菜单与悬浮球使用相同的边缘吸附逻辑

## v1.0.2（2026-08-10）

发布版整理：
1. 精简脚本内功能开发注释（发布规范：只保留功能说明与节标题）
2. 统一主题注释口径（blue/pink 互补强调色说明）

## v1.0.1（2026-08-07）

修复已知 BUG：
1. 修复面板重建时 document 级监听器累积泄漏
2. 修复弹幕按钮重建时旧监听器未清理

## v1.0.0（2026-08-07）

正式开源发布，核心功能：
1. 弹幕 +1 快捷回复
2. 评论区悬停
3. 小尾巴
4. 一键点赞
